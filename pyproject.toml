[project]
name = "audio-ad-generator"
version = "0.1.0"
description = "Simple audio ad generator web l"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.136.3",
    "openai>=1.0.0",
    "elevenlabs>=1.0.0",
    "python-dotenv>=1.0.0",
]
